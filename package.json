{
  "name": "ngmp",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "build": "babel . --out-dir dist --copy-files",
    "prestart": "rimraf dist && npm run build",
    "start": "node dist/app.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "nodemon --exec babel-node app.js"
  },
  "author": "Mikalai Lapatkin <mikalai_lapatkin@epam.com>",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "babel-preset-stage-2": "^6.24.1",
    "babel-register": "^6.26.0",
    "nodemon": "^1.17.3"
  }
}
